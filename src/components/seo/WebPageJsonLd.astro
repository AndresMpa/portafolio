---
// src/components/seo/WebPageJsonLd.astro

import { person } from "../../data/person";
import type { WebPageJsonLdProps } from "../../types";

const {
  title,
  description,
  canonical,
} = Astro.props as WebPageJsonLdProps;
---

<script
  is:inline
  type="application/ld+json"
  set:html={JSON.stringify({
    "@context":"https://schema.org",

    "@type":"WebPage",

    "@id":canonical,

    url:canonical,

    name:title,

    description,

    isPartOf:{
      "@id":"https://andresmpa.netlify.app/#website"
    },

    about:{
      "@id":person.id
    }
  })}
/>
